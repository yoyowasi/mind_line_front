// schedule_tab.dart
import 'package:flutter/material.dart';

class DaliyTab extends StatelessWidget {
  const DaliyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('일기 탭'));
  }
}
